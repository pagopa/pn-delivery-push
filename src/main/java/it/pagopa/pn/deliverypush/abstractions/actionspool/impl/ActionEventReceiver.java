package it.pagopa.pn.deliverypush.abstractions.actionspool.impl;
/*

import com.fasterxml.jackson.databind.ObjectMapper;
import it.pagopa.pn.deliverypush.PnDeliveryPushConfigs;
import it.pagopa.pn.deliverypush.temp.mom.consumer.SqsEventReceiver;
import org.springframework.stereotype.Component;
import software.amazon.awssdk.services.sqs.SqsClient;

import java.util.Collections;

@Component
public class ActionEventReceiver extends SqsEventReceiver{

    public ActionEventReceiver(PnDeliveryPushConfigs cfg, SqsClient sqs, ObjectMapper objMapper, ActionsEventHandler handler) {
        super(
                sqs,
                cfg.getTopics().getScheduledActions(),
                objMapper,
                Collections.singletonList( handler),
                Collections.singletonList( ActionEventType.ACTION_GENERIC )
            );
    }
}
*/
